import type { RunnerProgress } from '@cpm/shared';

export function parseProgress(_output: string): Partial<RunnerProgress> {
  // TODO: Parse cc output to extract progress information
  return {};
}
